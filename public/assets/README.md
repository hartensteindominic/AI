# Sample licensed assets

These SVG placeholders stand in for the full voxel pack until real binaries are uploaded.

On license consume, the API returns `downloadUrl` pointing here when the app is served from the same origin (or when `ASSET_BASE_URL` is set).
